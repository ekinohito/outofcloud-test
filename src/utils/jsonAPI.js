export async function postEmail(email) {
    //const res = await fetch('https://example.com/api/email', { method: "POST", body: JSON.stringify({email})})
    console.log(`Отправленный E-mail: ${email}`)
    return null
}